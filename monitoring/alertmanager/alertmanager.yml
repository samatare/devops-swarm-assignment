global: { resolve_timeout: 5m }
route: { group_by: [alertname], receiver: default, routes: [{ match: { severity: critical }, receiver: critical }] }
receivers: [{ name: default }, { name: critical }]